APP_STL := c++_shared
# NDK version definition is omitted here because it depends on minSdkVersion
# Use gradle to define APP_PLATFORM (called ANDROID_PLATFORM there)
# APP_PLATFORM := android-<minSdkVersion>
APP_ABI := armeabi-v7a arm64-v8a x86 x86_64
